subscription {
    notifyNewInstrumentDTO {
        id
        name
        ...on ShareDTO {
            price
        }
        ...on OptionDTO {
            callPrice
            putPrice
        }
    }
}
